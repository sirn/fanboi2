language: python
dist: trusty
sudo: false

matrix:
  include:
    - sudo: true
      services:
        - docker
      env:
        - POSTGRESQL_TEST_DATABASE=postgresql://postgres@localhost:5432/fanboi2_docker
      before_install:
        - docker build -t forloopend/fanboi2 .
      before_script:
        - psql -c "create database fanboi2_docker;" -U postgres
      script:
        - docker run forloopend/fanboi2 make test

    - python: "3.6"
      cache: pip
      before_script:
        - psql -c "create database fanboi2_docker;" -U postgres
      env:
        - POSTGRESQL_TEST_DATABASE=postgresql://postgres@localhost:5432/fanboi2
      install:
        - make .build/.build-test
      script:
        - make test

notifications:
  email:
    recipients:
      - build@fanboi.ch
    on_success: always
    on_failure: always
