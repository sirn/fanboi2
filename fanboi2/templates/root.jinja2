{% extends "layout.jinja2" %}
{% block content %}
  {% for board in boards %}
    <div id="board-{{ board.obj.slug }}" class="well">
      <h3><a href="{{ request.resource_url(board) }}">{{ board.obj.title }}</a></h3>
    </div>
  {% endfor %}
{% endblock %}